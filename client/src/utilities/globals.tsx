import { HoneytokenType } from './typing';

export class Globals {
  public static honeytokenTypes = [
    { id: HoneytokenType.Text, name: 'text file' },
    { id: HoneytokenType.API, name: 'api' },
  ];
}
