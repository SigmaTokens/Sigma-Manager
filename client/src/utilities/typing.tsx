export enum OS {
  Windows = 'Windows',
  Linux = 'Linux',
  Mac = 'Mac',
}

export enum Connection {
  IP = 'ip',
  Domain = 'domain',
}

export enum HoneytokenType {
  Text = 'text',
  API = 'api',
}
